#include "base64.h"
#include "cmps112-rsa.h"

#include <stdio.h>
#include <string.h>

void encrypt_message(
    rsa_key_t* key,
    uint8_t* message_in,
    uint8_t* message_out
) {
    (void)key;
    (void)message_in;
    (void)message_out;
}

void decrypt_message(
    rsa_key_t* key,
    uint8_t* message_in,
    uint8_t* message_out
) {
    (void)key;
    (void)message_in;
    (void)message_out;
}
